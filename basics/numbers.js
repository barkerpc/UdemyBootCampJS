let age = 37
let factor = 1/7
let dogYears = (age + 1) * factor
console.log(dogYears)

// Calculates the test score percentage

// total questions on the test
let totalQuestions = 100

let correctAnswers = 89
let percent = (correctAnswers / totalQuestions) * 100

console.log ('Percentage:' + percent + '%')